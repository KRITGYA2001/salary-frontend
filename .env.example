# Copy to .env and adjust. .env is git-ignored.
VITE_DEV_API_TARGET=http://localhost:8080
VM_HOST=140.238.231.135
VM_USER=ubuntu
# Path to the private key (never commit the key itself)
SSH_KEY_PATH=~/.ssh/acme_vm.key
